name: Deploy Access Hub

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'access-hub/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Access Hub
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: 'pnpm-lock.yaml'
      
      - name: Install all dependencies
        working-directory: .
        run: pnpm install --frozen-lockfile
      
      - name: Build Access Hub
        working-directory: access-hub
        env:
          VITE_ACCESS_API_URL: https://access-api.idling.app
          VITE_AUTH_API_URL: ${{ secrets.VITE_AUTH_API_URL || 'https://auth.idling.app' }}
          VITE_CUSTOMER_API_URL: ${{ secrets.VITE_CUSTOMER_API_URL || 'https://customer-api.idling.app' }}
        run: pnpm build
      
      - name: Deploy to Cloudflare Pages
        id: deploy
        working-directory: access-hub
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          # Create project if it doesn't exist (suppress error if already exists)
          pnpm exec wrangler pages project create access-hub --production-branch=main 2>/dev/null || echo "Project already exists, continuing with deployment..."
          # Build outputs to ../dist/access-hub (relative to access-hub directory)
          pnpm exec wrangler pages deploy ../dist/access-hub --project-name=access-hub --branch=main
      
      - name: Rollback on Failure
        if: failure()
        run: |
          echo "✗ Deployment failed - manual rollback required"
          echo "Please check Cloudflare dashboard and rollback if needed"
          exit 1
      
      - name: Deployment Summary
        run: |
          echo "## 🔐 Access Hub Deployed!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The Access Hub frontend has been deployed to Cloudflare Pages." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Frontend URL:** \`https://access.idling.app\`" >> $GITHUB_STEP_SUMMARY
          echo "**Backend API:** \`https://access-api.idling.app\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Features:" >> $GITHUB_STEP_SUMMARY
          echo "- View all system roles" >> $GITHUB_STEP_SUMMARY
          echo "- View all permissions" >> $GITHUB_STEP_SUMMARY
          echo "- Clean, minimal UI" >> $GITHUB_STEP_SUMMARY

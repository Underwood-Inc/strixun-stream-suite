{
  "name": "strixun-url-shortener",
  "version": "1.0.0",
  "description": "Cloudflare Worker for URL shortening with OTP authentication, click analytics, and standalone web interface - Strixun Stream Suite",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.1",
  "scripts": {
    "build:decrypt": "node scripts/bundle-decrypt.js",
    "build:otp-core": "node scripts/bundle-otp-core.js",
    "build:otp-login": "pnpm --filter @strixun/otp-login build",
    "build:all": "pnpm build:decrypt && pnpm build:otp-core && pnpm build:otp-login",
    "predeploy": "pnpm build:all",
    "predeploy:prod": "pnpm build:decrypt && pnpm build:otp-core && pnpm build:otp-login",
    "predev": "pnpm build:all",
    "dev": "wrangler dev",
    "dev:watch": "pnpm build:all && wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "tail:prod": "wrangler tail --env production"
  },
  "dependencies": {
    "@strixun/api-framework": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.19.12"
  }
}

{
  "name": "@strixun/customer-lookup",
  "version": "1.0.0",
  "description": "Shared utility for fetching customer data by customerId across all projects",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "types": "./index.ts",
      "default": "./index.ts"
    }
  },
  "files": [
    "*.ts",
    "*.md"
  ],
  "dependencies": {
    "@strixun/service-client": "workspace:*"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^4.0.16",
    "@vitest/coverage-v8": "^4.0.16"
  }
}

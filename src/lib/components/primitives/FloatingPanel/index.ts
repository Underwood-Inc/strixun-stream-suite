/**
 * FloatingPanel Component
 * 
 * A floating panel that overlays content (doesn't shift it).
 * Supports expand/collapse and horizontal resizing.
 */

export { default } from './FloatingPanel.svelte';

